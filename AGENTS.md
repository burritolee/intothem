# INTOTHEM 프로젝트 작업 규칙

## 배포 승인 원칙

- 로컬 파일 수정, 기능 구현, 로컬 테스트와 배포 전 검증까지는 별도의 배포 요청 없이 진행할 수 있다.
- 사용자가 현재 요청에서 명시적으로 `배포해줘`, `배포하자`, `운영 사이트에 반영해줘` 등 배포 의사를 밝히기 전에는 배포하지 않는다.
- 명시적인 배포 요청이 없으면 GitHub 원격 저장소에 push·웹 업로드·커밋하지 않고, Vercel 배포·재배포·운영 환경 변경도 하지 않는다.
- 구현이 끝나면 먼저 테스트 결과와 배포 준비 상태를 사용자에게 알리고 배포 요청을 기다린다.
- 이전 대화에서 받은 배포 허가는 이후 작업에 계속 적용되는 포괄적 허가로 간주하지 않는다. 각 변경 묶음마다 새 배포 요청이 필요하다.
- 사용자가 배포를 요청하면 배포 직전에 필요한 테스트를 완료하고, 테스트가 통과한 변경만 GitHub와 Vercel에 반영한다.
- 긴급 수정이나 사소한 문구 변경도 위 원칙의 예외로 취급하지 않는다.

